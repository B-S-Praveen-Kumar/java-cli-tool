# java-cli-tool
A developer CLI tool for automating Java project setup. Generate projects, packages, classes, interfaces, enums, and main files with built-in validation for naming conventions and duplicate detection.
